enum AuthMode { login, register }

enum AuthRole {
  siswa,
  ortu,
  // tamu,
}

enum OtpVia {
  email,
  sms,
  wa,
}

enum DialogType {
  success,
  info,
  warning,
  error,
}

enum EventPTNType {
  selectPTN,
  selectJurusan,
}

enum StatePTNType {
  selectPTN,
  selectJurusan,
}

enum JenisTO {
  utbk,
  ujianSekolah,
  anbk,
  stan,
}
